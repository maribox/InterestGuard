import type { YoutubeVideo } from "../types";

export function testVideos(videolist: YoutubeVideo[]) {
    for (let video of videolist) {
        // send redquest to opemanai api, update "block" variablemimI

    }
}